# AGENTS

このリポジトリのテキストファイルは UTF-8 として扱う。

- テキストファイルを読むときは UTF-8 を前提にする。
- PowerShell で読むときは `Get-Content -Encoding UTF8` のように明示する。
- テキストファイルを書き込むときや更新するときも UTF-8 を保つ。
- 日本語を含む `.tex`、`.md`、`.txt`、ソースファイルは特に文字化けに注意する。
